# NPM Configuration
# Standardize npm behavior across environments

# Use exact versions in package-lock.json
save-exact=false

# Require package-lock.json for installs
package-lock=true

# Use legacy peer deps resolution if needed
legacy-peer-deps=false

# Audit level
audit-level=moderate

# Fund message
fund=false
